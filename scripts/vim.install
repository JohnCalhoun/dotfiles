#! /bin/bash

dir=$1
home=$2

cd $home/.vim/bundle 
rm -rf $home/.vim/bundle/*

for package in $(cat $dir/configs/vim.text); do
	echo "cloning $package into $home/.vim/bundle"
	git clone $package
done

cd $dir 
